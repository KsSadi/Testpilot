# Browser Automation Service Configuration
# Copy this file to .env.recorder and edit the values

# ===========================================
# SERVER CONFIGURATION
# ===========================================

# Your VPS IP address or domain name (REQUIRED for VPS)
# This is used to generate the viewer URL for users
SERVER_HOST=your-server-ip-or-domain

# Port for the HTTP API (default: 3031)
RECORDER_PORT=3031

# ===========================================
# VPS MODE SETTINGS
# ===========================================

# Set to true when running on a VPS without GUI
VPS_MODE=true

# Enable VNC for remote browser access (recommended for VPS)
USE_VNC=true

# Force headless mode (only if VNC is disabled)
# When USE_VNC=true, browser will be visible through VNC
HEADLESS=false

# ===========================================
# CHROME / PUPPETEER SETTINGS
# ===========================================

# Path to Chrome executable (usually auto-detected)
# Common paths:
#   Ubuntu/Debian: /usr/bin/google-chrome-stable
#   CentOS/RHEL: /usr/bin/google-chrome
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable

# ===========================================
# VNC PORT RANGES
# ===========================================

# VNC server ports start from this number (internal)
# VNC_PORT_START=5900

# noVNC (web client) ports start from this number
# Users will access: http://SERVER_HOST:6080/vnc.html
# NOVNC_PORT_START=6080

# ===========================================
# RESOURCE LIMITS
# ===========================================

# Maximum concurrent recording sessions
# Each session uses ~500-700MB RAM
# For 8GB RAM VPS: recommended max 8-10 sessions
# MAX_SESSIONS=10

# Session timeout in minutes (auto-cleanup)
# SESSION_TIMEOUT=5
